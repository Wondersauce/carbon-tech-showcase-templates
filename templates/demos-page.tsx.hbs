import { {{pascalCase name}} } from "@/registry/{{baseType}}/{{kebabCase name}}";

export const metadata = {
  title: "{{pascalCase name}} Demo",
};

export default function Page() {
  return <{{pascalCase name}} />;
}
