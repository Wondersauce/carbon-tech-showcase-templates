import {
  RegistryEntry,
  //BASE_URL
} from "../schema";

export const registryEntry: RegistryEntry = {
  name: "{{kebabCase name}}",
  type: "{{type}}",
  files: ["{{baseType}}/{{kebabCase name}}.tsx"],
  dependencies: ["@carbon/react", "@carbon/icons-react"],
  registryDependencies: [
    //`${BASE_URL}/registry-dependency.json`,
  ],
};
