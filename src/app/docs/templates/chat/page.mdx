import Preview from "@/src/components/preview/Preview";
import CodeBlock from "@/src/components/codeBlock/CodeBlock";

export const metadata = {
  title: "Chat Documentation",
};

# Chat

<Preview previewURL="/demos/chat" />

## Installation

<CodeBlock
  className={"not-prose"}
  children={`npx shadcn@latest add ${process.env.NEXT_PUBLIC_REGISTRY_URL}/chat-01.json`}
/>
